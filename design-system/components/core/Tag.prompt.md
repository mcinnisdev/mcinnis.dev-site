Mono chip for stack/tech metadata. Sentence-cased content, never uppercase.

```jsx
<Tag>Azure Functions</Tag><Tag tone="accent">Open Source</Tag>
```

Use `tone="inverse"` inside ink sections. Wrap groups in a flex row with `gap: var(--space-2)`.
