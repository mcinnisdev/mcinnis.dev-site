Icon-only square control for secondary affordances (menu toggle, social links, copy).

```jsx
<IconButton icon="github" label="GitHub" href="https://github.com/mcinnisdev" variant="quiet" />
```

Variants: `quiet` (default, tints on hover), `outline` (on paper cards), `accent` (rare). Always pass `label`.
