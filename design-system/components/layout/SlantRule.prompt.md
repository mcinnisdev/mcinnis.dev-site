Decorative hatch band at the logo's 18.4° angle. Use once or twice per page — as a divider between major sections or a texture strip beside a hero.

```jsx
<SlantRule height={12} density={7} tone="accent" />
```
